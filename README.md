# Consulta PJe (simples)

Aplicação web simples para consultar comunicações do PJe pelo número do processo.

## Como executar

```bash
npm install
npm start
```

Abra no navegador:

- http://localhost:3000

## Endpoint

- `GET /processo/:numero`
